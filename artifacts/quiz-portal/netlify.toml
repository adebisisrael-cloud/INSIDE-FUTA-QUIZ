[build]
  command = "pnpm install && pnpm --filter quiz-portal run build"
  publish = "artifacts/quiz-portal/dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200